# Public URL only, never tokens or passwords.
# Server-side upstream for the same-origin Formula Rescue connector.
# Default works without .env.local. No secrets here.
FORMULARESCUE_API_URL=https://formula-rescue-api-gold.vercel.app
# Optional local backend: FORMULARESCUE_API_URL=http://127.0.0.1:8000
