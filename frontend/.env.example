# Local unified API, when Next.js and FastAPI both run on this laptop.
FORMULARESCUE_API_URL=http://127.0.0.1:8001
# Vercel Services generates SPONTAN_BACKEND_URL via vercel.json at runtime.
# Do not enter SPONTAN_BACKEND_URL manually or use NEXT_PUBLIC_ for it.
